# 今天这顿

每天三个饭点自动打开，只推荐一道菜，点一下跳到外卖 App 下单。

设计文档：`docs/superpowers/specs/2026-08-22-meal-recommender-design.md`

## 它不做什么

- 不代替你下单或支付 —— 只给推荐和跳转链接，最后一步你自己完成
- 不抓取任何外卖平台数据 —— 候选池由你手动录入
- 没有服务器 —— 数据只存在你这台手机的浏览器里

## 为什么是这个形状

这个 app 没有账号、没有后端、没有推送服务器，只有三条你自己设的 iOS 快捷指令
和一份存在浏览器本地的候选池。这不是偷懒，是有意为之：一顿饭的推荐值不上一整套
账号体系和云端存储。代价是几处需要你自己维护的地方 —— 手动录入候选池、手动配置
快捷指令、手动记得导出备份 —— README 把这些都写清楚了，因为它们不会在界面上
提醒你。

## 首次使用

### 1. 部署

这是一个纯静态站点，零构建步骤，直接推到 GitHub Pages 即可：

1. 把仓库推到 GitHub，例如 `xiaoyu-studio/meal`
2. 仓库 Settings → Pages，把来源设为 `main` 分支根目录
3. 几分钟后访问 `https://xiaoyu-studio.github.io/meal/`

当前开发在 `feat/v1-implementation` 分支上进行；何时合并到 `main` 并推送到远程，
由你自己决定和执行。

### 2. 添加到主屏幕（必做）

用 **Safari** 打开上面的地址 → 分享按钮 → 「添加到主屏幕」。

这一步不是为了好看：Safari 的智能防跟踪（ITP）会在网站 7 天无访问后清理它的
脚本可写存储（包括 IndexedDB），**但已安装到主屏的网页豁免这条规则**。
不装到主屏，你的候选池和几个月攒下的用餐历史迟早会被 Safari 静默清空，
而且不会有任何提示。

### 3. 录入候选池

点页面底部「候选池」。每家店需要：

1. 在美团或饿了么 App 里找到这家店
2. 点「分享」→ 复制链接
3. 回到候选池，填店名、选平台、粘贴链接
4. 在该店卡片下方逐道添加菜品

先录 5 家就能开始用，20–30 家会明显更好用。

### 4. 配置三条快捷指令

打开 iOS「快捷指令」App → 底部「自动化」→ 「新建自动化」→ 「特定时间」。

建三条，每条的动作都选「打开 URL」，并把「运行前询问」关掉：

| 时间 | URL |
|---|---|
| 08:00 | `https://xiaoyu-studio.github.io/meal/?slot=breakfast` |
| 11:30 | `https://xiaoyu-studio.github.io/meal/?slot=lunch` |
| 17:30 | `https://xiaoyu-studio.github.io/meal/?slot=dinner` |

时间按自己作息调整。

## 日常使用

到饭点手机会自动打开推荐页。点「去下单」跳到外卖 App 完成支付。
不想吃就点右上角「换一个」，一顿最多换两次。

下一顿打开时会补问上一顿怎么样 —— 一次点击就好。

## 备份

候选池页面顶部有「导出备份」，会下载一个 JSON 文件，存进「文件」App 即可。
**建议每月导出一次。** 数据只在这一台手机上，没有别的副本。

页面只会说「已生成 …，请在「文件」App 里确认」，不会说「已导出」—— 浏览器有没有
真的落盘，网页这边看不到。请每次都去「文件」App 里确认那个文件真的在。

## 图标

主屏图标由 `tools/make-icons.mjs` 从 `icon.svg` 的同一套形状生成
（`apple-touch-icon.png` / `icon-192.png` / `icon-512.png`）。改了 `icon.svg`
之后重新跑一次即可：

```bash
node tools/make-icons.mjs
```

脚本只用 Node 自带的 zlib 手写 PNG，是一次性资源生成，不引入任何运行时依赖。
位图是必须的：iOS 不认 SVG 的 `apple-touch-icon`，取不到位图时会拿页面截图
当主屏图标。

## 本地开发

ES Modules 无法从 `file://` 加载，必须走 HTTP：

```bash
python -m http.server 8000
```

然后打开 `http://localhost:8000/index.html`。

## 测试

纯逻辑模块（`config` / `dates` / `observations` / `recommender` / `snapshot`）
零依赖，用 Node 自带的测试运行器：

```bash
node --test
```

`store.js` 与 `ui-*.js` 碰浏览器 API，不在自动化测试范围内，由下面的真机验收覆盖。

## 调推荐手感

所有可调常数都在 `src/config.js` 里，改完刷新页面即生效。
最可能需要调的是 `FATIGUE_TAU_DISH`（多久不腻，默认 7 天）和
`W_TASTE` / `W_VALUE`（好吃与便宜的权重，默认 0.7 / 0.3）。

## iPhone 真机验收清单（spec §9.2）

以下几项自动化测试和桌面浏览器都无法覆盖，需要在真实 iPhone 上逐条确认。
**尚未验收 —— 下面全部是未勾选的待办事项**，不是已完成的报告。

- [ ] **1. 添加到主屏后能以独立窗口打开** —— Safari 打开
  `https://xiaoyu-studio.github.io/meal/` → 分享 → 添加到主屏幕 →
  从主屏图标启动，确认没有 Safari 地址栏
- [ ] **2. 三条快捷指令到点真的弹出对应饭点的页面** —— 把某条自动化的时间
  临时改到两分钟后，锁屏等待
- [ ] **3. 点「去下单」真的唤起外卖 App 并落在正确店铺页**
- [ ] **4. 杀掉 Safari 并重启手机后，候选池与历史数据仍在** —— 当天就能做完。
  这条只验证 IndexedDB 能扛住进程被杀和系统重启，**不验证 ITP 豁免**（见下一条）
- [ ] **5. 连续 7 天以上完全不打开，再打开时数据仍在** —— 这条才是验证 ITP
  豁免的那一条，是整个零后端方案成立的前提。Safari 的清理阈值是「7 天无访问」，
  所以它没有快捷做法：**必须真的搁置 7 天**，期间一次都不能打开（包括快捷指令
  自动弹出的那几次 —— 验收这一周请把三条自动化临时关掉，否则每天都在刷新访问时间，
  这条永远测不出来）。
  **若这条失败**，说明 ITP 豁免未如预期生效，spec §3.4 的零后端假设不成立 ——
  停止后续使用并回到 spec，评估升级为「轻后端 + Bark 推送」（spec §11）
- [ ] **6. 导出 JSON 能存入「文件」App，删掉一家店后再导入，数据完整还原** ——
  页面只说「已生成」，请务必到「文件」App 里确认文件真的存在

### 本轮修复新增的验收项

以下几条是最终代码评审后修复的路径，同样只能在真机上确认：

- [ ] **7. 主屏图标是那个橙色圆环图标，不是页面截图** —— 重新添加到主屏后确认
  （若仍是旧图标，先从主屏删除再重新添加，iOS 会缓存图标）
- [ ] **8. 连着两天推到同一道菜时，第二天的评分记在昨天那顿上** ——
  两天都推到同一道菜后点评分，去候选池看该菜的「吃过 N 次」，
  确认第三天打开时仍会补问第二天那顿
- [ ] **9. 补问某道菜时，这一顿不会又推同一道菜**（候选池只剩一道菜时除外）
- [ ] **10. 候选池里能把「已拉黑」改回「未知/放心」，改完该店的菜重新出现在推荐里**
- [ ] **11. 导出时切到别的 App 再切回来，页面不会卡死在空白卡片上** ——
  验证事务被中断后失败态与「重试」按钮会正常出现

### 以下四项是此前任务里明确留下的验证缺口，一并列入本清单

这四项不是新发现的问题，而是 Task 14 / Task 15 完成时诚实记录下来的、自动化和
桌面浏览器测试都没能覆盖的路径。在真机验收时请一并确认：

- [ ] **反馈浮层「填实付价 → 保存」路径**：打开反馈浮层，点「填实付价」，
  输入一个价格，点保存，确认价格被持久化保存（Task 14 遗留：桌面浏览器
  session 中浏览器的输入事件模拟不稳定，这条路径从未被真实点击验证过；
  反馈浮层里的其它交互都已验证过）
- [ ] **候选池「删店」按钮的真实点击**：在候选池页面上真的点一下某家店的
  「删店」按钮，确认店铺及其菜品被删除（Task 15 遗留：此前只通过直接调用
  store 层函数、以及结构相同但共享同一个事件委托处理器的「删菜」按钮
  间接验证过，删店按钮本身从未被真实点击过）
- [ ] **存储失败回退界面**：在 `index.html` 和 `pool.html` 上各触发一次
  存储失败，确认 `#failure` 区块正确显示、文案合理、点「重试」有效
  （Task 15 遗留：此前的测试环境里没能人为触发出存储失败场景，这条路径
  在两个页面上都从未被真实触发过）
- [ ] **手机宽度下的布局检查**：在 iPhone 实际屏幕宽度下查看 `index.html`
  和 `pool.html`，确认没有横向溢出、按钮/文字没有被遮挡或挤压
  （Task 15 遗留：此前的所有验证都只在桌面浏览器宽度下进行过）

### 验收结果

尚未验收。验收完成后请在此处补充验收日期与全部十五条的实际结果。

注意第 5 条（ITP 豁免）必须搁置 7 天才能得出结论，所以整轮验收最快也要跨一周。
其余十四条可以在头两天内做完 —— 第 8 条需要连着两天，其余当天即可。
