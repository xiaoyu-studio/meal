node_modules/
.DS_Store
.claude/settings.local.json
# SDD 工作区：计划、评审 diff、agent 报告。仓库要发到公开的 GitHub Pages，
# 一次 `git add .` 就会把整个过程记录一起推上去。
.superpowers/

# 个人待办。是工作笔记不是产品的一部分，而且仓库是公开的 ——
# 没必要把「哪些还没做完、哪条验收没过」挂到公网上。
# 代价：不进版本库就没有备份，也不跨机器同步。
TODO.md
