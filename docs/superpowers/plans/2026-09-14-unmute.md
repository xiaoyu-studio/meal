# 「别再推这个」的撤销出口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 候选池里「已静音 · MM-DD」后面加一个「取消」按钮，点了写一条 `unmuted` 事件，该菜立刻回到没静音过的状态。

**Architecture:** 事件日志只追加：新增 `unmuted` 事件（`slot: null`）。纯函数 `buildMutedIndex` 改为「最近一次 `muted` 之后若有 `ts` 不小于它的 `unmuted`，就不算静音」，返回形状不变，所以 `muteOf` / `rankCandidates` / 候选池标记都自动跟上。界面只动 `ui-pool.js` 的菜品行模板和 `#shops` 的事件委托，外加一条 CSS。

**Tech Stack:** 原生 ES modules、IndexedDB、`node --test`，零依赖零构建。

**Spec:** `docs/superpowers/specs/2026-09-14-unmute-design.md`

## Global Constraints

- 零运行时依赖、零构建步骤；`package.json` 只允许 `name` / `private` / `type`
- `src/config.js src/dates.js src/observations.js src/recommender.js src/snapshot.js` 不得触碰时钟、随机数、DOM、存储
- 插进 `innerHTML` 的用户数据必须过 `esc()`，**属性值里也要**
- 面向用户的文案一律简体中文
- 不升 `SNAPSHOT_VERSION`，不迁移老数据，不改 `store.js`、`snapshot.js`、`ui-today.js`、`index.html`、`muteOf` 公式、`MUTE_TAU_DAYS`
- `unmuted` 事件格式：`{ id, ts, slot: null, dishId, type: 'unmuted', value: null }`，写入走 `appendEvent({ slot: null, dishId, type: 'unmuted' })`
- toast 文案：`已取消静音「<菜名>」`，取不到菜名时 `已取消静音`
- **不改 `docs/acceptance-checklist.md`**：第 24 条等 `fix/resume-stale-card` 合并进 `main` 后另行添加（spec §5.3 时序约束）
- 提交说明结尾：`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- 行号仅供参考，一律按引用的原文定位
- 分支 `feat/unmute`，不合并、不推送

---

## 文件结构

| 文件 | 操作 | 职责 |
|---|---|---|
| `src/observations.js` | 修改 `buildMutedIndex` | 静音索引认 `unmuted` |
| `test/observations.test.js` | 追加 7 条 | spec §5.1 第 1–7 条 |
| `test/recommender.test.js` | 追加 1 条 | spec §5.1 第 8 条（端到端） |
| `src/ui-pool.js` | 修改模板、导入、事件委托 | 「取消」按钮与点击分支 |
| `css/style.css` | 追加 1 条规则 | 按钮字号跟随那一行 |
| `docs/superpowers/specs/2026-08-26-browse-and-implicit-signals-design.md` | 两处注记 | §5 表、§7.2 |

---

### Task 1: `buildMutedIndex` 认 `unmuted`

**Files:**
- Modify: `src/observations.js`（`buildMutedIndex`，约第 200–214 行）
- Test: `test/observations.test.js`（文件末尾追加）
- Test: `test/recommender.test.js`（文件末尾追加）

**Interfaces:**
- Consumes: 无
- Produces: `buildMutedIndex(events) → Map<dishId, 'YYYY-MM-DD'>`，签名与返回形状不变；Task 2 的候选池直接用它

- [ ] **Step 1: 在 `test/observations.test.js` 末尾追加 7 条测试**

文件顶部已有 `at(dayOffset, hour)`（本地 2026-08-(22+dayOffset) hour:00）与 `ev(type, dishId, ts, slot = 'lunch', value = null)`，直接复用。

```js
// ---- 2026-09-14：取消静音（spec 2026-09-14 §3.2、§5.1）----

test('静音后取消 → 不在静音索引里', () => {
  const idx = buildMutedIndex([
    ev('muted', 'd1', at(0, 12)),
    ev('unmuted', 'd1', at(4, 12), null),
  ]);
  assert.equal(idx.has('d1'), false);
});

test('静音与取消 ts 相等 → 算已取消', () => {
  const ts = at(0, 12);
  const idx = buildMutedIndex([
    ev('muted', 'd1', ts),
    ev('unmuted', 'd1', ts, null),
  ]);
  assert.equal(idx.has('d1'), false);
});

test('取消只影响被取消的那道菜', () => {
  const idx = buildMutedIndex([
    ev('muted', 'd1', at(0, 12)),
    ev('muted', 'd2', at(1, 12)),
    ev('unmuted', 'd1', at(2, 12), null),
  ]);
  assert.equal(idx.has('d1'), false);
  assert.equal(idx.get('d2'), '2026-08-23');
});

test('取消之后再静音 → 以再静音那天为准', () => {
  const idx = buildMutedIndex([
    ev('muted', 'd1', at(0, 12)),
    ev('unmuted', 'd1', at(4, 12), null),
    ev('muted', 'd1', at(9, 12)),
  ]);
  assert.equal(idx.get('d1'), '2026-08-31');
});

test('先取消后静音 → 静音有效', () => {
  const idx = buildMutedIndex([
    ev('unmuted', 'd1', at(0, 12), null),
    ev('muted', 'd1', at(4, 12)),
  ]);
  assert.equal(idx.get('d1'), '2026-08-26');
});

test('只有取消没有静音 → 不在静音索引里', () => {
  const idx = buildMutedIndex([
    ev('unmuted', 'd1', at(0, 12), null),
  ]);
  assert.equal(idx.has('d1'), false);
});

test('slot 为 null 的 unmuted 不影响 reduceObservations', () => {
  const base = [
    ev('recommended', 'd1', at(0, 12)),
    ev('clicked', 'd1', at(0, 12) + 60000),
  ];
  const withUnmute = [...base, ev('unmuted', 'd1', at(0, 13), null)];
  assert.deepEqual(reduceObservations(withUnmute), reduceObservations(base));
});
```

- [ ] **Step 2: 在 `test/recommender.test.js` 末尾追加 1 条测试**

文件顶部已有 `NOW`（本地 2026-08-22 12:00）与 `DAY`，已导入 `rankCandidates`。

```js
test('静音后取消的菜，排序与分数和从没静音过完全相同', () => {
  const dishes = [
    { id: 'a', shopId: 's1', name: 'A', refPrice: 10, slots: ['lunch'], tags: [] },
    { id: 'b', shopId: 's1', name: 'B', refPrice: 20, slots: ['lunch'], tags: [] },
  ];
  const shops = [{ id: 's1', name: 'S', hygiene: 'unknown' }];
  const events = [
    { id: 'm1', ts: NOW - DAY, slot: 'lunch', dishId: 'a', type: 'muted', value: null },
    { id: 'u1', ts: NOW - DAY + 60000, slot: null, dishId: 'a', type: 'unmuted', value: null },
  ];
  const args = { dishes, shops, slot: 'lunch', now: NOW, random: () => 0.5 };
  assert.deepEqual(
    rankCandidates({ ...args, events }),
    rankCandidates({ ...args, events: [] }),
  );
});
```

- [ ] **Step 3: 跑测试，确认按预期失败**

Run: `node --test`

Expected: 151 条中 **4 条失败**，其余通过：
- 「静音后取消 → 不在静音索引里」
- 「静音与取消 ts 相等 → 算已取消」
- 「取消只影响被取消的那道菜」
- 「静音后取消的菜，排序与分数和从没静音过完全相同」

「取消之后再静音」「先取消后静音」「只有取消没有静音」「slot 为 null 的 unmuted 不影响 reduceObservations」四条在旧代码上通过是**预期的**（spec §5.1：它们防新规则的错误实现，不是判别旧代码）。若失败数不是 4，停下检查测试是否抄错。

- [ ] **Step 4: 改写 `buildMutedIndex`**

在 `src/observations.js` 找到原文：

```js
/**
 * 每道菜最近一次被按下「别再推这个」的本地日期键。
 * 直接从原始事件流取，不经过观察值 —— 静音是对菜的表态，
 * 不属于任何一顿饭。
 */
export function buildMutedIndex(events) {
  const out = new Map();
  for (const e of events) {
    if (e.type !== 'muted') continue;
    const key = localDateKey(e.ts);
    const prev = out.get(e.dishId);
    if (prev === undefined || key > prev) out.set(e.dishId, key);
  }
  return out;
}
```

整段替换为：

```js
/**
 * 每道菜最近一次被按下「别再推这个」、且之后没被取消的本地日期键。
 * 直接从原始事件流取，不经过观察值 —— 静音是对菜的表态，
 * 不属于任何一顿饭。
 *
 * 先后比 ts 而不比日期：同一天先静音后取消，日期相同分不出先后
 * （spec 2026-09-14 §3.2）。ts 相等时算已取消。
 */
export function buildMutedIndex(events) {
  const lastMuted = new Map();   // dishId -> ts
  const lastUnmuted = new Map(); // dishId -> ts
  for (const e of events) {
    const latest = e.type === 'muted' ? lastMuted
      : e.type === 'unmuted' ? lastUnmuted
      : null;
    if (!latest) continue;
    const prev = latest.get(e.dishId);
    if (prev === undefined || e.ts > prev) latest.set(e.dishId, e.ts);
  }

  const out = new Map();
  for (const [dishId, mutedTs] of lastMuted) {
    const unmutedTs = lastUnmuted.get(dishId);
    if (unmutedTs !== undefined && unmutedTs >= mutedTs) continue;
    out.set(dishId, localDateKey(mutedTs));
  }
  return out;
}
```

（原先按日期键取最大，现在按 `ts` 取最大再转日期键 —— 对只有 `muted` 的情况结果相同，已有测试「buildMutedIndex 取每道菜最近一次 muted 的日期」继续通过。）

- [ ] **Step 5: 跑测试，确认全部通过**

Run: `node --test`
Expected: 151 条全部通过，0 失败。

- [ ] **Step 6: 提交**

```bash
git add src/observations.js test/observations.test.js test/recommender.test.js
git commit -m "feat: buildMutedIndex 认 unmuted 事件，取消之后的菜不再算静音

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 候选池「取消」按钮，外加旧 spec 注记

**Files:**
- Modify: `src/ui-pool.js`（导入约第 6–9 行；菜品行模板约第 132–135 行；`#shops` 事件委托约第 291–296 行）
- Modify: `css/style.css`（`.dish-muted` 规则之后，约第 301 行）
- Modify: `docs/superpowers/specs/2026-08-26-browse-and-implicit-signals-design.md`（§5 表约第 136 行，§7.2 约第 182 行）

**Interfaces:**
- Consumes: Task 1 的 `buildMutedIndex`（已被 `ui-pool.js` 导入，无需改导入）；`store.js` 现有的 `appendEvent({ slot, dishId, type, value = null, targetTs = null, dateKey = null })`
- Produces: 按钮属性 `data-unmute-dish="<dishId>"`

- [ ] **Step 1: 导入 `appendEvent`**

`src/ui-pool.js` 原文：

```js
import {
  loadAll, putShop, putDish, deleteShop, deleteDish, setHygiene,
  newId, exportSnapshot, importSnapshot,
} from './store.js';
```

改为：

```js
import {
  loadAll, putShop, putDish, deleteShop, deleteDish, setHygiene,
  newId, exportSnapshot, importSnapshot, appendEvent,
} from './store.js';
```

- [ ] **Step 2: 菜品行模板加按钮**

原文：

```js
            const mutedHtml = mutedKey
              ? `<span class="dish-muted">已静音 · ${esc(mutedKey.slice(5))}</span>`
              : '';
```

改为：

```js
            // 「取消」挨着它改的那个状态放，不放右栏：右栏是 nowrap，
            // 再塞一个按钮会把菜名挤没，而且跟「删」挨着容易点错（spec 2026-09-14 §4.1）。
            const mutedHtml = mutedKey
              ? `<span class="dish-muted">已静音 · ${esc(mutedKey.slice(5))}` +
                `<button class="link" data-unmute-dish="${esc(d.id)}" type="button">取消</button></span>`
              : '';
```

- [ ] **Step 3: `#shops` 事件委托加分支**

原文：

```js
el('shops').addEventListener('click', async (e) => {
  const button = e.target.closest('button');
  if (!button) return;

  try {
    if (button.dataset.delDish) {
```

改为：

```js
el('shops').addEventListener('click', async (e) => {
  const button = e.target.closest('button');
  if (!button) return;

  try {
    // 不弹确认框：「删」要确认是因为不可逆；取消静音点错了，
    // 下次划到这道菜再按一次「别再推这个」就回来了（spec 2026-09-14 §2.1）。
    if (button.dataset.unmuteDish) {
      const dishId = button.dataset.unmuteDish;
      const { dishes } = await loadAll();
      const name = dishes.find((d) => d.id === dishId)?.name;
      // slot 写 null：候选池没有「当前饭点」，也让归约 Pass 2 挂不到任何一顿上。
      await appendEvent({ slot: null, dishId, type: 'unmuted' });
      await render();
      showToast(name ? `已取消静音「${name}」` : '已取消静音');
      return;
    }

    if (button.dataset.delDish) {
```

写入失败会落到这个监听器末尾已有的 `catch`（顶部 `#io-msg` 显示「操作失败，请稍后重试。」），不需要另写。

- [ ] **Step 4: CSS**

`css/style.css` 原文：

```css
.dish-muted {
  display: block;
  margin-top: 0.15rem;
  color: var(--accent);
  font-size: 0.78rem;
}
```

在它后面紧接着追加：

```css

/* 「已静音 · MM-DD」后面的「取消」。.link 默认 0.85rem，比这一行大一号会显得突兀。 */
.dish-muted .link {
  font-size: inherit;
  padding: 0 0 0 0.4rem;
}
```

- [ ] **Step 5: 跑测试，确认没有破坏**

Run: `node --test`
Expected: 151 条全部通过（UI 层无自动化测试，这一步只确认纯模块没被误改）。

- [ ] **Step 6: 浏览器实测（spec §5.2）**

`.claude/launch.json` 里的 `meal-fresh`（端口 8042）。浏览器模块缓存是这个项目反复踩过的坑：导航时带 `?bust=<新值>`。

1. 打开 `http://localhost:8042/pool.html?bust=u1`，在页面里执行（清出干净测试库并写入一条静音）：

```js
const store = await import('/src/store.js?bust=u1');
const all = ['breakfast', 'lunch', 'dinner'];
await store.importSnapshot({ version: 1, exportedAt: Date.now(),
  shops: [{ id: 's1', name: '甲店', platform: 'eleme', link: 'https://example.com/a', hygiene: 'unknown', note: '' }],
  dishes: [
    { id: 'd1', shopId: 's1', name: '红烧牛肉面', refPrice: 28, tags: ['面'], slots: ['lunch', 'dinner'], active: true },
    { id: 'd2', shopId: 's1', name: '番茄鸡蛋盖饭', refPrice: 22, tags: ['饭'], slots: all, active: true },
  ],
  events: [{ id: 'm1', ts: Date.now() - 86400000, slot: 'lunch', dishId: 'd1', type: 'muted', value: null }] });
location.reload();
```

2. 确认「红烧牛肉面」下显示「已静音 · MM-DD」加灰色下划线「取消」；「番茄鸡蛋盖饭」没有这一行
3. 点「取消」（真实点击）：那一行消失，底部 toast「已取消静音「红烧牛肉面」」
4. 刷新页面：那一行仍不在；执行 `(await (await import('/src/store.js?bust=u1')).loadAll()).events` 确认多出一条 `type: 'unmuted'`、`slot: null`、`dishId: 'd1'`
5. 重新写入第 1 步的快照，`resize_window` 设为 375×812，确认：`document.documentElement.scrollWidth <= 375`（无横向溢出），且「取消」按钮的 `getBoundingClientRect().top` 与「已静音」文字在同一行（与所在 `.dish-muted` 的 top 差不超过 4px）
6. `read_console_messages` 无报错；测完 `resize_window` 恢复 `desktop`

任何一项不符，停下报告，不要提交。

- [ ] **Step 7: 旧 spec 注记**

`docs/superpowers/specs/2026-08-26-browse-and-implicit-signals-design.md` §5 表原文：

```markdown
| `muted` | `null` | **新增**，按「别再推这个」时写入 |
```

改为：

```markdown
| `muted` | `null` | **新增**，按「别再推这个」时写入 |
| `unmuted` | `null` | **2026-09-14 新增**，候选池点「取消」时写入，`slot` 为 `null`。见 `2026-09-14-unmute-design.md` |
```

§7.2 原文：

```markdown
设 `d` 为距该菜最近一次 `muted` 事件的天数（自然日，设备本地时区）：
```

改为：

```markdown
设 `d` 为距该菜最近一次 `muted` 事件的天数（自然日，设备本地时区）：

> 2026-09-14 修订：「最近一次 `muted`」之后若有 `ts` 不小于它的 `unmuted`，该菜视为从未静音（`mute = 1`）。
> 见 `2026-09-14-unmute-design.md` §3.2。
```

- [ ] **Step 8: 提交**

```bash
git add src/ui-pool.js css/style.css docs/superpowers/specs/2026-08-26-browse-and-implicit-signals-design.md
git commit -m "feat: 候选池「已静音」后加「取消」按钮，写 unmuted 事件并提示；旧 spec 加注记

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## 计划之外、合并前要做的

- `fix/resume-stale-card` 合并进 `main` 后，在 `docs/acceptance-checklist.md` 加第 24 条（spec §5.3），并注明第 15 条需重验
- `TODO.md` 第 5 条标注进度（该文件不进 git）
